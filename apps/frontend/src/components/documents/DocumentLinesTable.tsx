import type { LogisticsDocumentLine } from '../../types/documents'

type Props = { lines: LogisticsDocumentLine[]; onEdit: (line: LogisticsDocumentLine) => void; onDeactivate: (line: LogisticsDocumentLine) => void }
export function DocumentLinesTable({ lines, onEdit, onDeactivate }: Props) {
  if (lines.length === 0) return <div className="rounded-2xl border border-dashed border-slate-300 p-4 text-sm text-slate-500">El documento no tiene líneas registradas.</div>
  return <div className="overflow-x-auto rounded-2xl border border-slate-200"><table className="min-w-full divide-y divide-slate-200 text-sm"><thead className="bg-slate-50 text-left text-xs font-semibold uppercase text-slate-500"><tr>{['#','Encomienda','Bulto','Parada','Descripción','Cantidad','Peso','Volumen','Referencia','Acciones'].map((h) => <th key={h} className="px-3 py-2">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{lines.map((line) => <tr key={line.id} className={!line.is_active ? 'bg-slate-50 text-slate-400' : ''}><td className="px-3 py-2 font-semibold">{line.line_number}</td><td className="px-3 py-2">{line.shipment_tracking_code ?? '—'}</td><td className="px-3 py-2">{line.package_code ?? '—'}</td><td className="px-3 py-2">{line.route_stop_sequence ?? '—'}</td><td className="px-3 py-2">{line.description}</td><td className="px-3 py-2">{line.quantity ?? '—'}</td><td className="px-3 py-2">{line.weight_kg ?? '—'}</td><td className="px-3 py-2">{line.volume_m3 ?? '—'}</td><td className="px-3 py-2">{line.reference_code ?? '—'}</td><td className="px-3 py-2"><div className="flex gap-2"><button onClick={() => onEdit(line)} className="rounded-lg border border-slate-200 px-2 py-1 font-semibold">Editar</button><button onClick={() => onDeactivate(line)} className="rounded-lg border border-slate-200 px-2 py-1 font-semibold text-slate-500">Desactivar</button></div></td></tr>)}</tbody></table></div>
}

import type { DriverPerformanceRow } from '../../types/reports'
import { formatNumber } from '../../utils/reportLabels'
import { EmptyReportState } from './EmptyReportState'
export function DriverPerformanceTable({ rows }: { rows: DriverPerformanceRow[] }) {
  if (!rows.length) return <EmptyReportState message="No hay conductores con datos para los filtros seleccionados." />
  return <div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200 text-sm"><thead className="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><tr>{['Conductor','Rutas total','Rutas completadas','Paradas total','Paradas completadas','Paradas fallidas','Encomiendas asignadas','Encomiendas entregadas','Incidencias total','Incidencias abiertas'].map((h) => <th key={h} className="px-3 py-3 font-bold">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map((row) => <tr key={row.driver_id ?? row.driver_name} className="hover:bg-slate-50"><td className="px-3 py-3 font-semibold text-slate-900">{row.driver_name ?? 'Sin nombre'}</td><td className="px-3 py-3">{formatNumber(row.routes_total)}</td><td className="px-3 py-3">{formatNumber(row.routes_completed)}</td><td className="px-3 py-3">{formatNumber(row.stops_total)}</td><td className="px-3 py-3">{formatNumber(row.stops_completed)}</td><td className="px-3 py-3">{formatNumber(row.stops_failed)}</td><td className="px-3 py-3">{formatNumber(row.shipments_assigned)}</td><td className="px-3 py-3">{formatNumber(row.shipments_delivered)}</td><td className="px-3 py-3">{formatNumber(row.incidents_total)}</td><td className="px-3 py-3">{formatNumber(row.incidents_open)}</td></tr>)}</tbody></table></div>
}

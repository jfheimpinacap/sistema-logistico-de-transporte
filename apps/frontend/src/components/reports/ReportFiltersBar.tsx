import type { ReactNode } from 'react'
import type { ReportFilters } from '../../types/reports'

type Props = { filters: ReportFilters; onChange: (filters: ReportFilters) => void; onApply: () => void; onClear: () => void; onRefresh: () => void; children?: ReactNode; showIsActive?: boolean }
export function ReportFiltersBar({ filters, onChange, onApply, onClear, onRefresh, children, showIsActive = true }: Props) {
  const setFilter = (key: string, value: string) => onChange({ ...filters, [key]: value })
  return <div className="rounded-3xl border border-slate-200 bg-white p-4 shadow-sm"><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-5"><label className="text-sm font-semibold text-slate-600">Desde<input type="date" value={filters.date_from ?? ''} onChange={(e: { target: { value: string } }) => setFilter('date_from', e.target.value)} className="mt-1 w-full rounded-xl border border-slate-200 px-3 py-2 text-sm" /></label><label className="text-sm font-semibold text-slate-600">Hasta<input type="date" value={filters.date_to ?? ''} onChange={(e: { target: { value: string } }) => setFilter('date_to', e.target.value)} className="mt-1 w-full rounded-xl border border-slate-200 px-3 py-2 text-sm" /></label>{children}{showIsActive ? <label className="text-sm font-semibold text-slate-600">Activo<select value={filters.is_active ?? 'all'} onChange={(e: { target: { value: string } }) => setFilter('is_active', e.target.value)} className="mt-1 w-full rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="all">Todos</option><option value="true">Activos</option><option value="false">Inactivos</option></select></label> : null}</div><div className="mt-4 flex flex-wrap gap-2"><button type="button" onClick={onApply} className="rounded-xl bg-slate-950 px-4 py-2 text-sm font-bold text-white hover:bg-slate-800">Aplicar</button><button type="button" onClick={onClear} className="rounded-xl border border-slate-200 px-4 py-2 text-sm font-bold text-slate-700 hover:bg-slate-50">Limpiar</button><button type="button" onClick={onRefresh} className="rounded-xl border border-cyan-200 bg-cyan-50 px-4 py-2 text-sm font-bold text-cyan-700 hover:bg-cyan-100">Refrescar</button></div></div>
}

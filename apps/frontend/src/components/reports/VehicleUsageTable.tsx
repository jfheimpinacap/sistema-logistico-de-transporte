import type { VehicleUsageRow } from '../../types/reports'
import { formatNumber, getReportLabel, vehicleStatusLabels } from '../../utils/reportLabels'
import { EmptyReportState } from './EmptyReportState'
export function VehicleUsageTable({ rows }: { rows: VehicleUsageRow[] }) {
  if (!rows.length) return <EmptyReportState message="No hay vehículos con datos para los filtros seleccionados." />
  return <div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200 text-sm"><thead className="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><tr>{['Patente','Tipo','Rutas total','Rutas completadas','Encomiendas asignadas','Peso total','Incidencias','Estado actual'].map((h) => <th key={h} className="px-3 py-3 font-bold">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map((row) => <tr key={row.vehicle_id ?? row.vehicle_plate} className="hover:bg-slate-50"><td className="px-3 py-3 font-semibold text-slate-900">{row.vehicle_plate ?? 'Sin patente'}</td><td className="px-3 py-3">{row.vehicle_type_name ?? '—'}</td><td className="px-3 py-3">{formatNumber(row.routes_total)}</td><td className="px-3 py-3">{formatNumber(row.routes_completed)}</td><td className="px-3 py-3">{formatNumber(row.shipments_assigned)}</td><td className="px-3 py-3">{formatNumber(row.total_weight_kg)} kg</td><td className="px-3 py-3">{formatNumber(row.incidents_total)}</td><td className="px-3 py-3"><span className="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold text-slate-700">{getReportLabel(row.current_status, vehicleStatusLabels)}</span></td></tr>)}</tbody></table></div>
}

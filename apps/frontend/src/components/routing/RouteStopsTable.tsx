import { RouteStopStatusBadge, getRouteStopTypeLabel } from './RouteStopStatusBadge'
import type { RouteStop } from '../../types/routing'

type Props = { stops: RouteStop[]; isLoading: boolean; onEdit: (stop: RouteStop) => void; onChangeStatus: (stop: RouteStop) => void; onDelete: (stop: RouteStop) => void }
function formatDateTime(value?: string | null) { return value ? new Date(value).toLocaleString('es-CL') : '—' }
export function RouteStopsTable({ stops, isLoading, onEdit, onChangeStatus, onDelete }: Props) {
  return <section className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-200 px-5 py-4"><h3 className="font-bold text-slate-950">Paradas</h3></div><div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200 text-left text-sm"><thead className="bg-slate-50 text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">Sec.</th><th className="px-4 py-3">Dirección</th><th className="px-4 py-3">Zona</th><th className="px-4 py-3">Tipo</th><th className="px-4 py-3">Estado</th><th className="px-4 py-3">Llegada planificada</th><th className="px-4 py-3">Contacto</th><th className="px-4 py-3">Teléfono</th><th className="px-4 py-3 text-right">Acciones</th></tr></thead><tbody className="divide-y divide-slate-100 bg-white text-slate-700">{isLoading ? <tr><td colSpan={9} className="px-4 py-7 text-center text-slate-500">Cargando paradas...</td></tr> : stops.length === 0 ? <tr><td colSpan={9} className="px-4 py-7 text-center text-slate-500">Esta ruta aún no tiene paradas.</td></tr> : stops.map((stop) => <tr key={stop.id} className="hover:bg-slate-50/80"><td className="px-4 py-3 font-semibold">{stop.sequence}</td><td className="px-4 py-3">{stop.address_label || '—'}</td><td className="px-4 py-3">{stop.zone_name || '—'}</td><td className="px-4 py-3">{getRouteStopTypeLabel(stop.stop_type)}</td><td className="px-4 py-3"><RouteStopStatusBadge value={stop.status} /></td><td className="px-4 py-3">{formatDateTime(stop.planned_arrival_time)}</td><td className="px-4 py-3">{stop.contact_name || '—'}</td><td className="px-4 py-3">{stop.contact_phone || '—'}</td><td className="px-4 py-3 text-right"><div className="flex justify-end gap-2"><button type="button" onClick={() => onEdit(stop)} className="rounded-lg border border-slate-200 px-3 py-1.5 text-xs font-semibold text-slate-700 hover:bg-slate-50">Editar</button><button type="button" onClick={() => onChangeStatus(stop)} className="rounded-lg border border-cyan-200 px-3 py-1.5 text-xs font-semibold text-cyan-700 hover:bg-cyan-50">Estado</button><button type="button" onClick={() => onDelete(stop)} disabled={!stop.is_active} className="rounded-lg border border-rose-200 px-3 py-1.5 text-xs font-semibold text-rose-700 hover:bg-rose-50 disabled:cursor-not-allowed disabled:border-slate-200 disabled:text-slate-300">Desactivar</button></div></td></tr>)}</tbody></table></div></section>
}

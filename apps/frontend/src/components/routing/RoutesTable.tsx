import { RouteStatusBadge } from './RouteStatusBadge'
import type { Route } from '../../types/routing'

type Props = { routes: Route[]; isLoading: boolean; selectedId?: string | number | null; onView: (route: Route) => void; onEdit: (route: Route) => void; onDelete: (route: Route) => void }
function formatDate(value?: string | null) { return value ? new Date(`${value}T00:00:00`).toLocaleDateString('es-CL') : '—' }
export function RoutesTable({ routes, isLoading, selectedId, onView, onEdit, onDelete }: Props) {
  return <section className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200 text-left text-sm"><thead className="bg-slate-50 text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">Código</th><th className="px-5 py-4">Nombre</th><th className="px-5 py-4">Fecha</th><th className="px-5 py-4">Conductor</th><th className="px-5 py-4">Vehículo</th><th className="px-5 py-4">Bodega origen</th><th className="px-5 py-4">Estado</th><th className="px-5 py-4">Encomiendas</th><th className="px-5 py-4">Bultos</th><th className="px-5 py-4 text-right">Acciones</th></tr></thead><tbody className="divide-y divide-slate-100 bg-white text-slate-700">{isLoading ? <tr><td colSpan={10} className="px-5 py-8 text-center text-slate-500">Cargando rutas...</td></tr> : routes.length === 0 ? <tr><td colSpan={10} className="px-5 py-8 text-center text-slate-500">No hay rutas para los filtros seleccionados.</td></tr> : routes.map((route) => <tr key={route.id} className={selectedId === route.id ? 'bg-cyan-50/60' : 'hover:bg-slate-50/80'}><td className="whitespace-nowrap px-5 py-4 font-semibold text-slate-950">{route.route_code || 'Autogenerado'}</td><td className="whitespace-nowrap px-5 py-4">{route.name}</td><td className="whitespace-nowrap px-5 py-4">{formatDate(route.route_date)}</td><td className="whitespace-nowrap px-5 py-4">{route.driver_name || '—'}</td><td className="whitespace-nowrap px-5 py-4">{route.vehicle_plate || '—'}</td><td className="whitespace-nowrap px-5 py-4">{route.origin_warehouse_name || '—'}</td><td className="whitespace-nowrap px-5 py-4"><RouteStatusBadge value={route.status} /></td><td className="whitespace-nowrap px-5 py-4">{route.route_shipments_count ?? route.total_shipments ?? 0}</td><td className="whitespace-nowrap px-5 py-4">{route.total_packages ?? 0}</td><td className="whitespace-nowrap px-5 py-4 text-right"><div className="flex justify-end gap-2"><button type="button" onClick={() => onView(route)} className="rounded-lg border border-cyan-200 px-3 py-1.5 text-xs font-semibold text-cyan-700 transition hover:bg-cyan-50">Ver detalle</button><button type="button" onClick={() => onEdit(route)} className="rounded-lg border border-slate-200 px-3 py-1.5 text-xs font-semibold text-slate-700 transition hover:bg-slate-50">Editar</button><button type="button" onClick={() => onDelete(route)} disabled={!route.is_active} className="rounded-lg border border-rose-200 px-3 py-1.5 text-xs font-semibold text-rose-700 transition hover:bg-rose-50 disabled:cursor-not-allowed disabled:border-slate-200 disabled:text-slate-300">Desactivar</button></div></td></tr>)}</tbody></table></div></section>
}

import { useEffect, useState } from 'react'
import type { FormEvent } from 'react'
import { routeStatusOptions } from './RouteStatusBadge'
import type { SelectOption } from '../../types/masterData'
import type { Route, RoutePayload } from '../../types/routing'

type Props = { title: string; route?: Route | null; warehouseOptions: SelectOption[]; driverOptions: SelectOption[]; vehicleOptions: SelectOption[]; isSaving: boolean; error?: string | null; onSubmit: (payload: RoutePayload) => void; onCancel: () => void }
type FormState = { route_code: string; name: string; description: string; route_date: string; planned_start_time: string; planned_end_time: string; origin_warehouse: string; driver: string; vehicle: string; status: string; estimated_distance_km: string; estimated_duration_minutes: string; notes: string; is_active: boolean }
function toLocalDateTime(value?: string | null) { return value ? value.slice(0, 16) : '' }
function initialState(route?: Route | null): FormState { return { route_code: route?.route_code ?? '', name: route?.name ?? '', description: route?.description ?? '', route_date: route?.route_date ?? new Date().toISOString().slice(0, 10), planned_start_time: toLocalDateTime(route?.planned_start_time), planned_end_time: toLocalDateTime(route?.planned_end_time), origin_warehouse: route?.origin_warehouse ? String(route.origin_warehouse) : '', driver: route?.driver ? String(route.driver) : '', vehicle: route?.vehicle ? String(route.vehicle) : '', status: route?.status ?? 'draft', estimated_distance_km: route?.estimated_distance_km ? String(route.estimated_distance_km) : '', estimated_duration_minutes: route?.estimated_duration_minutes ? String(route.estimated_duration_minutes) : '', notes: route?.notes ?? '', is_active: route?.is_active ?? true } }
function clean(value: string) { return value.trim() === '' ? null : value.trim() }
export function RouteForm({ title, route, warehouseOptions, driverOptions, vehicleOptions, isSaving, error, onSubmit, onCancel }: Props) {
  const [form, setForm] = useState<FormState>(() => initialState(route))
  useEffect(() => setForm(initialState(route)), [route])
  function setField<K extends keyof FormState>(field: K, value: FormState[K]) { setForm((current) => ({ ...current, [field]: value })) }
  function handleSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); onSubmit({ route_code: clean(form.route_code), name: form.name.trim(), description: form.description.trim(), route_date: form.route_date, planned_start_time: clean(form.planned_start_time), planned_end_time: clean(form.planned_end_time), origin_warehouse: clean(form.origin_warehouse), driver: clean(form.driver), vehicle: clean(form.vehicle), status: form.status, estimated_distance_km: clean(form.estimated_distance_km), estimated_duration_minutes: form.estimated_duration_minutes ? Number(form.estimated_duration_minutes) : null, notes: form.notes.trim(), is_active: form.is_active }) }
  const inputClass = 'mt-1 w-full rounded-xl border border-slate-200 px-3 py-2 text-sm text-slate-700 outline-none transition focus:border-cyan-400 focus:ring-2 focus:ring-cyan-100'
  const missingMasters = warehouseOptions.length === 0 || driverOptions.length === 0 || vehicleOptions.length === 0
  return <div className="fixed inset-0 z-40 flex items-center justify-center bg-slate-950/40 p-4"><div className="max-h-[92vh] w-full max-w-5xl overflow-y-auto rounded-3xl bg-white shadow-xl"><form onSubmit={handleSubmit}><div className="border-b border-slate-200 px-6 py-4"><h3 className="text-lg font-bold text-slate-950">{title}</h3>{missingMasters ? <p className="mt-2 text-sm text-amber-700">Primero debes crear bodegas, conductores, vehículos o direcciones en Maestros.</p> : null}{error ? <p className="mt-2 rounded-xl bg-rose-50 px-3 py-2 text-sm font-medium text-rose-700">{error}</p> : null}</div><div className="grid gap-4 px-6 py-5 md:grid-cols-2 lg:grid-cols-3"><label className="text-sm font-semibold text-slate-700">Código<input className={inputClass} value={form.route_code} onChange={(event: any) => setField('route_code', event.target.value)} placeholder="Autogenerado si queda vacío" /></label><label className="text-sm font-semibold text-slate-700">Nombre<input required className={inputClass} value={form.name} onChange={(event: any) => setField('name', event.target.value)} /></label><label className="text-sm font-semibold text-slate-700">Fecha<input required type="date" className={inputClass} value={form.route_date} onChange={(event: any) => setField('route_date', event.target.value)} /></label><label className="text-sm font-semibold text-slate-700">Inicio planificado<input type="datetime-local" className={inputClass} value={form.planned_start_time} onChange={(event: any) => setField('planned_start_time', event.target.value)} /></label><label className="text-sm font-semibold text-slate-700">Término planificado<input type="datetime-local" className={inputClass} value={form.planned_end_time} onChange={(event: any) => setField('planned_end_time', event.target.value)} /></label><label className="text-sm font-semibold text-slate-700">Estado<select className={inputClass} value={form.status} onChange={(event: any) => setField('status', event.target.value)}>{routeStatusOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="text-sm font-semibold text-slate-700">Bodega origen<select className={inputClass} value={form.origin_warehouse} onChange={(event: any) => setField('origin_warehouse', event.target.value)}><option value="">Sin bodega</option>{warehouseOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="text-sm font-semibold text-slate-700">Conductor<select className={inputClass} value={form.driver} onChange={(event: any) => setField('driver', event.target.value)}><option value="">Sin conductor</option>{driverOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="text-sm font-semibold text-slate-700">Vehículo<select className={inputClass} value={form.vehicle} onChange={(event: any) => setField('vehicle', event.target.value)}><option value="">Sin vehículo</option>{vehicleOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="text-sm font-semibold text-slate-700">Distancia estimada km<input type="number" step="0.01" className={inputClass} value={form.estimated_distance_km} onChange={(event: any) => setField('estimated_distance_km', event.target.value)} /></label><label className="text-sm font-semibold text-slate-700">Duración estimada min<input type="number" className={inputClass} value={form.estimated_duration_minutes} onChange={(event: any) => setField('estimated_duration_minutes', event.target.value)} /></label><label className="mt-7 flex items-center gap-2 text-sm font-semibold text-slate-700"><input type="checkbox" checked={form.is_active} onChange={(event: any) => setField('is_active', event.target.checked)} />Activa</label><label className="md:col-span-2 lg:col-span-3 text-sm font-semibold text-slate-700">Descripción<textarea rows={2} className={inputClass} value={form.description} onChange={(event: any) => setField('description', event.target.value)} /></label><label className="md:col-span-2 lg:col-span-3 text-sm font-semibold text-slate-700">Notas<textarea rows={3} className={inputClass} value={form.notes} onChange={(event: any) => setField('notes', event.target.value)} /></label></div><div className="flex justify-end gap-3 border-t border-slate-200 px-6 py-4"><button type="button" onClick={onCancel} className="rounded-xl border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50">Cancelar</button><button type="submit" disabled={isSaving} className="rounded-xl bg-slate-950 px-4 py-2 text-sm font-semibold text-white hover:bg-slate-800 disabled:cursor-wait disabled:bg-slate-400">{isSaving ? 'Guardando...' : 'Guardar ruta'}</button></div></form></div></div>
}

import { useEffect, useState } from 'react'
import type { FormEvent } from 'react'
import { routeStopStatusOptions, routeStopTypeOptions } from './RouteStopStatusBadge'
import type { SelectOption } from '../../types/masterData'
import type { RouteStop, RouteStopPayload, RoutingId } from '../../types/routing'

type Props = { routeId: RoutingId; stop?: RouteStop | null; addressOptions: SelectOption[]; zoneOptions: SelectOption[]; isSaving: boolean; error?: string | null; onSubmit: (payload: RouteStopPayload) => void; onCancel: () => void }
type FormState = { sequence: string; address: string; zone: string; stop_type: string; status: string; planned_arrival_time: string; contact_name: string; contact_phone: string; instructions: string; notes: string; is_active: boolean }
function toLocalDateTime(value?: string | null) { return value ? value.slice(0, 16) : '' }
function initialState(stop?: RouteStop | null): FormState { return { sequence: String(stop?.sequence ?? 1), address: stop?.address ? String(stop.address) : '', zone: stop?.zone ? String(stop.zone) : '', stop_type: stop?.stop_type ?? 'delivery', status: stop?.status ?? 'pending', planned_arrival_time: toLocalDateTime(stop?.planned_arrival_time), contact_name: stop?.contact_name ?? '', contact_phone: stop?.contact_phone ?? '', instructions: stop?.instructions ?? '', notes: stop?.notes ?? '', is_active: stop?.is_active ?? true } }
function clean(value: string) { return value.trim() === '' ? null : value.trim() }
export function RouteStopForm({ routeId, stop, addressOptions, zoneOptions, isSaving, error, onSubmit, onCancel }: Props) {
  const [form, setForm] = useState<FormState>(() => initialState(stop))
  useEffect(() => setForm(initialState(stop)), [stop])
  function setField<K extends keyof FormState>(field: K, value: FormState[K]) { setForm((current) => ({ ...current, [field]: value })) }
  function handleSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); onSubmit({ route: routeId, sequence: Number(form.sequence || 1), address: clean(form.address), zone: clean(form.zone), stop_type: form.stop_type, status: form.status, planned_arrival_time: clean(form.planned_arrival_time), contact_name: form.contact_name.trim(), contact_phone: form.contact_phone.trim(), instructions: form.instructions.trim(), notes: form.notes.trim(), is_active: form.is_active }) }
  const inputClass = 'mt-1 w-full rounded-xl border border-slate-200 px-3 py-2 text-sm text-slate-700 outline-none transition focus:border-cyan-400 focus:ring-2 focus:ring-cyan-100'
  return <div className="fixed inset-0 z-40 flex items-center justify-center bg-slate-950/40 p-4"><div className="max-h-[92vh] w-full max-w-4xl overflow-y-auto rounded-3xl bg-white shadow-xl"><form onSubmit={handleSubmit}><div className="border-b border-slate-200 px-6 py-4"><h3 className="text-lg font-bold text-slate-950">{stop ? 'Editar parada' : 'Crear parada'}</h3>{addressOptions.length === 0 ? <p className="mt-2 text-sm text-amber-700">Primero debes crear direcciones en Maestros.</p> : null}{error ? <p className="mt-2 rounded-xl bg-rose-50 px-3 py-2 text-sm font-medium text-rose-700">{error}</p> : null}</div><div className="grid gap-4 px-6 py-5 md:grid-cols-2"><label className="text-sm font-semibold text-slate-700">Secuencia<input required type="number" min="1" className={inputClass} value={form.sequence} onChange={(event: any) => setField('sequence', event.target.value)} /></label><label className="text-sm font-semibold text-slate-700">Dirección<select className={inputClass} value={form.address} onChange={(event: any) => setField('address', event.target.value)}><option value="">Sin dirección</option>{addressOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="text-sm font-semibold text-slate-700">Zona<select className={inputClass} value={form.zone} onChange={(event: any) => setField('zone', event.target.value)}><option value="">Sin zona</option>{zoneOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="text-sm font-semibold text-slate-700">Tipo<select className={inputClass} value={form.stop_type} onChange={(event: any) => setField('stop_type', event.target.value)}>{routeStopTypeOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="text-sm font-semibold text-slate-700">Estado<select className={inputClass} value={form.status} onChange={(event: any) => setField('status', event.target.value)}>{routeStopStatusOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="text-sm font-semibold text-slate-700">Llegada planificada<input type="datetime-local" className={inputClass} value={form.planned_arrival_time} onChange={(event: any) => setField('planned_arrival_time', event.target.value)} /></label><label className="text-sm font-semibold text-slate-700">Contacto<input className={inputClass} value={form.contact_name} onChange={(event: any) => setField('contact_name', event.target.value)} /></label><label className="text-sm font-semibold text-slate-700">Teléfono<input className={inputClass} value={form.contact_phone} onChange={(event: any) => setField('contact_phone', event.target.value)} /></label><label className="md:col-span-2 text-sm font-semibold text-slate-700">Instrucciones<textarea rows={2} className={inputClass} value={form.instructions} onChange={(event: any) => setField('instructions', event.target.value)} /></label><label className="md:col-span-2 text-sm font-semibold text-slate-700">Notas<textarea rows={2} className={inputClass} value={form.notes} onChange={(event: any) => setField('notes', event.target.value)} /></label><label className="flex items-center gap-2 text-sm font-semibold text-slate-700"><input type="checkbox" checked={form.is_active} onChange={(event: any) => setField('is_active', event.target.checked)} />Activa</label></div><div className="flex justify-end gap-3 border-t border-slate-200 px-6 py-4"><button type="button" onClick={onCancel} className="rounded-xl border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50">Cancelar</button><button type="submit" disabled={isSaving} className="rounded-xl bg-slate-950 px-4 py-2 text-sm font-semibold text-white hover:bg-slate-800 disabled:cursor-wait disabled:bg-slate-400">{isSaving ? 'Guardando...' : 'Guardar parada'}</button></div></form></div></div>
}

import { getRouteShipmentStatusLabel } from './RouteStopStatusBadge'
import type { RouteShipment } from '../../types/routing'

type Props = { assignments: RouteShipment[]; isLoading: boolean; onDelete?: (assignment: RouteShipment) => void }
function formatDateTime(value?: string | null) { return value ? new Date(value).toLocaleString('es-CL') : '—' }
export function RouteShipmentsTable({ assignments, isLoading, onDelete }: Props) {
  return <section className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-200 px-5 py-4"><h3 className="font-bold text-slate-950">Encomiendas asignadas</h3></div><div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200 text-left text-sm"><thead className="bg-slate-50 text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">Tracking</th><th className="px-4 py-3">Destinatario</th><th className="px-4 py-3">Parada</th><th className="px-4 py-3">Estado</th><th className="px-4 py-3">Asignada</th><th className="px-4 py-3 text-right">Acciones</th></tr></thead><tbody className="divide-y divide-slate-100 bg-white text-slate-700">{isLoading ? <tr><td colSpan={6} className="px-4 py-7 text-center text-slate-500">Cargando asignaciones...</td></tr> : assignments.length === 0 ? <tr><td colSpan={6} className="px-4 py-7 text-center text-slate-500">Esta ruta aún no tiene encomiendas asignadas.</td></tr> : assignments.map((assignment) => <tr key={assignment.id} className="hover:bg-slate-50/80"><td className="px-4 py-3 font-semibold text-slate-950">{assignment.shipment_tracking_code || assignment.shipment}</td><td className="px-4 py-3">{assignment.shipment_recipient_name || '—'}</td><td className="px-4 py-3">{assignment.stop_sequence ? `#${assignment.stop_sequence}` : 'Sin parada'}</td><td className="px-4 py-3">{getRouteShipmentStatusLabel(assignment.status)}</td><td className="px-4 py-3">{formatDateTime(assignment.assigned_at)}</td><td className="px-4 py-3 text-right">{onDelete ? <button type="button" onClick={() => onDelete(assignment)} disabled={!assignment.is_active} className="rounded-lg border border-rose-200 px-3 py-1.5 text-xs font-semibold text-rose-700 hover:bg-rose-50 disabled:cursor-not-allowed disabled:border-slate-200 disabled:text-slate-300">Desactivar</button> : <span className="text-xs text-slate-400">Entrega en próximos prompts</span>}</td></tr>)}</tbody></table></div></section>
}
